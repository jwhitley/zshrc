# isnumeric -- numerical test predicate
#   
isnumeric () {
  if (( $# >= 1 )); then
    local numtest
    let "numtest = $1 + 1"
    (( numtest > 1 )) && echo 'true'
  else
    ohno "isnumeric: No parameter provided."
  fi
}

# Local Variables:
# mode: shell-script
# End:
